import React, {FC} from 'react';

const GalleryError:FC = () => {
    return (
        <div>
            You entered an invalid id, please try another one :(
        </div>
    );
};

export default GalleryError;